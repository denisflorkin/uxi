# Node.js with React
# Build a Node.js project that uses React.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript


# some comment goes here

# and another one

# and another one

# and another

# and another one


pool:
  vmImage: 'Ubuntu 16.04'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    npm install
    # IS THIS STILL VALID?
    # npm dedupe # because reasons, without that zopfli(jest>request) fails and all goes to hell
  displayName: 'Install node modules'

- script: |
    npm run test
  displayName: 'Run Tests'

- script: |
    npm run pack
  displayName: 'Pack for npmjs registry publish'

# - task: CopyFiles@2
#     displayName: 'Publish artifact'
#     inputs:
#         SourceFolder: build
#         Contents: 'DataStore*.dacpac'
#         TargetFolder: '$(build.artifactstagingdirectory)/artifact'

# Publish Build Artifacts
# Publish build artifacts to Azure Artifacts/TFS or a file share

- task: PublishBuildArtifacts@1
  displayName: 'Publish artifact'
  inputs:
    pathtoPublish: '$(Build.ArtifactStagingDirectory)/build'
    artifactName: 'packed'
    #publishLocation: 'Container' # Options: container, filePath
    #targetPath: # Required when publishLocation == FilePath
    #parallel: false # Optional
    #parallelCount: # Optional
